class documentoFirebase {

    constructor(idFirestore, horaStatus, idJS, seccion, status) {
        this.idFirestore = idFirestore;
        this.horaStatus = horaStatus;
        this.idJS = idJS;
        this.seccion = seccion;
        this.status = status;
    }

 
}